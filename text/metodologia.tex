\section{Metodologia}

A condução deste trabalho segue uma abordagem estruturada em três etapas principais: levantamento de dados, modelagem matemática do problema e experimentação por meio de resolvedores matemáticos. A seguir, descrevemos cada uma dessas fases em detalhes.

\subsection{Levantamento de Dados e Cenários}

Inicialmente, realiza-se o levantamento dos principais parâmetros e cenários utilizados nos modelos de coleta de dados assistida por VANTs, conforme apresentado em trabalhos como (citar os trabalhos). São coletadas informações sobre os modos de coleta (hovering, fly-by e híbrido), características dos sensores, limitações energéticas dos UAVs, padrões de cobertura, e tipos de topologia das redes de sensores sem fio.

Além disso, define-se o cenário base da simulação, como a área de cobertura, número e distribuição dos sensores, tipo de UAV (asa fixa ou rotativo), tempo máximo de voo e parâmetros de consumo energético. Esses dados são utilizados como entrada para a etapa de modelagem matemática.

\subsection{Modelagem Matemática}

Com os dados obtidos, é formulado o modelo matemático para o problema de coleta de dados por UAVs. O problema é estruturado como uma variante de roteamento com restrições energéticas, inspirado em modelos de roteamento capacitado e problemas de cobertura com múltiplos alvos.

A função objetivo pode ser definida como a minimização da energia total consumida ou da idade média da informação (Age of Information - AoI), sujeita a restrições de:

\begin{itemize}
\item capacidade energética dos UAVs;
\item cobertura total ou parcial dos sensores;
\item tempo máximo de missão;
\item conectividade entre pontos de coleta e retorno à base.
\end{itemize}

Com base em modelos da literatura, como em \cite{liu2018uavWSN} e \cite{zhang2018drlrvc}, variáveis contínuas e binárias são utilizadas para representar trajetórias, ativação de nós e consumo energético. O modelo é resolvido usando programação inteira mista (MILP).

\subsection{Testes e Validação com Resolvedor}

A fase final consiste na implementação da modelagem matemática em linguagem compatível com o resolvedor Gurobi, por meio de scripts em Python. Os testes computacionais visam avaliar o desempenho do modelo em diferentes cenários, variando parâmetros como o número de sensores, alcance de comunicação e número de UAVs.

São analisadas as métricas:

\begin{itemize}
\item energia total consumida por missão;
\item tempo de cobertura dos sensores;
\item distância percorrida;

\item quantidade de sensores visitados; \colorbox{yellow!25}{não tem nos resultados mas poderia ser uma boa métrica}
\item tempo computacional de resolução. \colorbox{yellow!25}{não tem nos resultados mas poderia ser uma boa métrica}
\end{itemize}

\subsubsection{Cenários}
Os experimentos foram conduzidos sobre um conjunto de cenários sintéticos que variam dois parâmetros independentes: quantidade de sensores implantados na área de monitoramento e o tamanho da área coberta. Para a quantidade de sensores, foram considerados os valores $n_s \in \{5, 10, 15, 20, 25, 30\}$, e para a dimensão da área quadrada foram adotados os tamanhos $L \in \{100, 200, 400, 600, 800, 1000\}$ metros. A combinação fatorial desses dois parâmetros resulta em 36 cenários distintos, denominados $P_{n_s\text{-}L}$ (ex.: $P_{10\text{-}400}$ para 10 sensores em área de 400×400 m).

Para cada cenário, as coordenadas $(x, y)$ dos sensores são geradas aleatoriamente e distribuídas de forma uniforme dentro da área quadrada de lado $L$, sendo fixadas em arquivos CSV para garantir reprodutibilidade. O posicionamento é mantido constante ao longo de todas as rodadas de simulação, de modo a isolar os efeitos da variação de escala sobre as métricas de desempenho.

Cada cenário é executado por 30 rodadas consecutivas, simulando missões periódicas do VANT. Em cada rodada:

\begin{enumerate}
\item O estado de \textit{Age of Information} (AoI) de todos os sensores é carregado do estado persistido pela rodada anterior, iniciando em zero na primeira rodada
\item O modelo de otimização é resolvido com horizonte temporal de 20 \textit{slots} de 10\,s (missão de 200\,s), com limite de tempo de 60\,s e tolerância de otimalidade de 1\% (\textit{MIPGap}). 
\item O VANT parte da base, realiza \textit{hover} sobre os sensores selecionados para coleta e retorna à base. 
\item O estado de AoI é atualizado: sensores visitados têm AoI zerada; os demais acumulam $+1$ por \textit{slot} sem visita.
\end{enumerate}
 

Ao término das 30 rodadas, o estado de AoI é descartado, garantindo independência entre execuções de cenários distintos. 

Após a execução de todos os cenários, os resultados são agregados calculando-se a média das 30 rodadas para cada combinação $(n_s, L)$. Os valores médios são analisados por meio de:
                                                                         
\begin{itemize}
\item \textbf{Gráficos de linha}: evolução de cada métrica em função do tamanho do mapa, com uma curva por quantidade de sensores.
\item \textbf{Mapas de calor}: visualização bidimensional da métrica em função do par $(n_s, L)$, permitindo identificar regiões de maior e menor custo operacional.
\end{itemize}

\begin{figure}[htbp]                                                                                                 
  \centering                                                                                                       
  \includegraphics[width=0.8\textwidth]{figs/aoi_vs_map.png}                                                       
  \caption{AoI média final em função do tamanho do mapa -- DJI Matrice 300 RTK.}
  \label{fig:aoi_vs_map}                                                                                           
\end{figure}                                                                                                         
                                                                                                                       
\begin{figure}[htbp]                                                                                                 
  \centering
  \includegraphics[width=0.8\textwidth]{figs/distance_vs_map.png}                                                  
  \caption{Distância total média em função do tamanho do mapa -- DJI Matrice 300 RTK.}
  \label{fig:distance_vs_map}                                                                                      
\end{figure}                                                                                                         
                                                                                                                      
\begin{figure}[htbp]                                                                                                 
  \centering
  \includegraphics[width=0.8\textwidth]{figs/energy_vs_map.png}
  \caption{Energia média consumida em função do tamanho do mapa -- DJI Matrice 300 RTK.}
  \label{fig:energy_vs_map}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/heatmap_aoi.png}
  \caption{Mapa de calor da AoI média final por quantidade de sensores e tamanho do mapa -- DJI Matrice 300 RTK.}
  \label{fig:heatmap_aoi}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/heatmap_distance.png}
  \caption{Mapa de calor da distância total média por quantidade de sensores e tamanho do mapa -- DJI Matrice 300 RTK.}
  \label{fig:heatmap_distance}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/heatmap_energy.png}
  \caption{Mapa de calor da energia média consumida por quantidade de sensores e tamanho do mapa -- DJI Matrice 300 RTK.}
  \label{fig:heatmap_energy}
\end{figure}

Os resultados obtidos são comparados com a literatura, sempre que possível, com o objetivo de validar a eficiência da abordagem proposta e explorar a sensibilidade do modelo a diferentes configurações.

\subsubsection{Configuração dos Drones}

Os experimentos foram conduzidos com dois modelos distintos de VANT, cujos parâmetros físicos são utilizados no modelo de potência de asa rotativa (Equação~7).

\paragraph{DJI Matrice 300 RTK.}
O primeiro conjunto de experimentos utiliza os parâmetros físicos do DJI Matrice 300 RTK, um drone de uso profissional amplamente empregado em aplicações industriais e de monitoramento. Para estes experimentos, a capacidade de bateria foi fixada em $E^{\max} = 50.000$\,J, representando um subconjunto da autonomia total do veículo com o objetivo de avaliar o comportamento do otimizador em condições mais restritivas de energia.

\paragraph{Parrot Anafi USA.}
O segundo conjunto de experimentos foi realizado com os parâmetros do Parrot Anafi USA, um drone compacto voltado para aplicações de segurança e mapeamento. Para este modelo, a capacidade de bateria utilizada foi $E^{\max} = 141.372$\,J, correspondendo à capacidade real da bateria do equipamento.

Os resultados do DJI Matrice 300 RTK são apresentados nas Figuras~\ref{fig:aoi_vs_map}--\ref{fig:heatmap_energy}. Os resultados para o Parrot Anafi USA são apresentados a seguir.

\subsubsection{Resultados: Parrot Anafi USA}

As figuras a seguir apresentam os resultados experimentais obtidos com o Parrot Anafi USA ($E^{\max} = 141.372$\,J), para os mesmos 36 cenários.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/anafi_aoi_vs_map.png}
  \caption{AoI média final em função do tamanho do mapa -- Parrot Anafi USA.}
  \label{fig:anafi_aoi_vs_map}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/anafi_distance_vs_map.png}
  \caption{Distância total média em função do tamanho do mapa -- Parrot Anafi USA.}
  \label{fig:anafi_distance_vs_map}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/anafi_energy_vs_map.png}
  \caption{Energia média consumida em função do tamanho do mapa -- Parrot Anafi USA.}
  \label{fig:anafi_energy_vs_map}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/anafi_heatmap_aoi.png}
  \caption{Mapa de calor da AoI média final por quantidade de sensores e tamanho do mapa -- Parrot Anafi USA.}
  \label{fig:anafi_heatmap_aoi}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/anafi_heatmap_distance.png}
  \caption{Mapa de calor da distância total média por quantidade de sensores e tamanho do mapa -- Parrot Anafi USA.}
  \label{fig:anafi_heatmap_distance}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/anafi_heatmap_energy.png}
  \caption{Mapa de calor da energia média consumida por quantidade de sensores e tamanho do mapa -- Parrot Anafi USA.}
  \label{fig:anafi_heatmap_energy}
\end{figure}